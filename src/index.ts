// 이 파일은 전체 라이브러리의 진입점(entry point) 역할을 한다.
// export * from "./components"; 구문은 components 폴더의 모든 export들을 재export한다.
// 이렇게 함으로써, 라이브러리 사용자는 이 라이브러리에서 제공하는 모든 컴포넌트를 import { Button, AnotherComponent } from 'your-library-name'와 같은 방식으로 한 번에 가져올 수 있도록 한다.

export * from "./ components";
